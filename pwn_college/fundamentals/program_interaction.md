# pwn.college - Fundamentals - Program Interaction

## level1
```
$ /challenge/embryoio_level1
```
`pwn.college{kSG1R5pVQkMf1QDn_8DwK4qsMvq.dFDLyYjNyEzW}`

## level2
```
$ /challenge/embryoio_level2
auxcpzno
```
`pwn.college{MVXfX2QBWZTHCT6T2Kqa9pBweGd.dJDLyYjNyEzW}`

## level3
```
$ /challenge/embryoio_level3 vnafzqlxec
```
`pwn.college{QXseS-utjdpJNYmTWrR0XUK0ANT.dNDLyYjNyEzW}`

## level4
```
$ env jffxnx=rinayyvroy /challenge/embryoio_level4
```
`pwn.college{0Mwc9K06KZgx_ZXY0AJOQ8fysNr.dRDLyYjNyEzW}`

## level5
Create a text file at `/tmp/cxxfve` with the text `lknfmoro`
``` 
$ /challenge/embryoio_level5 < /tmp/cxxfve
```
`pwn.college{YlOwdcUfJI8o1ERyPduZybHDqr3.dVDLyYjNyEzW}`

## level6
```
$ /challenge/embryoio_level6 > /tmp/vqfvry
$ cat /tmp/vqfvry
```
`pwn.college{kNHBtzyFtsXZQ9PW4lSRmVdzwSt.dZDLyYjNyEzW}`

## level7
```
$ env -i /challenge/embryoio_level7
```
`pwn.college{Y7ts9VIzcgVkHRNQsrGzWimpNwK.ddDLyYjNyEzW}`

## level8
```
$ echo '/challenge/embryoio_level8' > solve.sh
$ chmod +x ./solve.sh
$ bash solve.sh
```
`pwn.college{siUhj9NdCMIWD3IllNZl3wJyToj.dhDLyYjNyEzW}`

## level9
```
$ echo '/challenge/embryoio_level9' > solve.sh
$ chmod +x ./solve.sh
$ bash solve.sh
uajzvqia
```
`pwn.college{0VsOtpFD8eaI8yo03xXWqieLact.dlDLyYjNyEzW}`

## level10
```
$ echo '/challenge/embryoio_level10 bojahpqwxu' > solve.sh
$ bash solve.sh
```
`pwn.college{wD8V_hTtkZvyqkHP-1lksjym7XG.QXwEDLyYjNyEzW}`

## level11
```
$ echo '/challenge/embryoio_level11' > solve.sh
$ env jinbxc=uuzezdplfx bash solve.sh
```
`pwn.college{EipZGkNQrSTsz3smyrcEhRCqm9t.QXxEDLyYjNyEzW}`

## level12
```
$ echo '/challenge/embryoio_level12' > solve.sh
$ echo "xnnmqwdy" > /tmp/kglepl
$ bash solve.sh < /tmp/kglepl
```
`pwn.college{8NbyqFdivjodTkLVK0gAn6COIcz.QXyEDLyYjNyEzW}`

## level13
```
$ echo '/challenge/embryoio_level13' > solve.sh
$ bash solve.sh > /tmp/qvxufv
$ cat /tmp/qvxufv
```
`pwn.college{0Wn4nFAl78B45x8V89cxWIdzq4n.QXzEDLyYjNyEzW}`

## level14
```
$ echo 'env -i echo '/challenge/embryoio_level13' > solve.sh' > solve.sh
$ bash solve.sh
```
`pwn.college{I1zJlqc_DjUWMQJB_BwRKhZmcil.QX0EDLyYjNyEzW}`

## level15
Run `$ ipython` to enter `ipython` environment. Type the following line-by-line:
```py
import subprocess
subprocess.Popen("/challenge/embryoio_level15", shell=False, stdout=subprocess.PIPE).stdout.read()
```
`pwn.college{0wgIVyesbHlPPGWvIlaixxRDCl6.QX1EDLyYjNyEzW}`

## level16
Run `$ ipython` to enter `ipython` environment. Type the following line-by-line:
```py
import subprocess
subprocess.Popen("/challenge/embryoio_level16", shell=False, stdout=subprocess.PIPE).stdout.read()
usaxszyn
```
`pwn.college{ExBB2ZL5QUbCS9Ch6ztbslm1CJ4.QX2EDLyYjNyEzW}`

## level17
Run `$ ipython` to enter `ipython` environment. Type the following line-by-line:
```py
import subprocess
subprocess.run(["/challenge/embryoio_level17","qmkhzrnnxd"]).stdout
```
`pwn.college{4iQ-ML165eAl2BmB8xEhD4SyD7_.QX3EDLyYjNyEzW}`

## level18
Run `$ ipython` to enter `ipython` environment. Type the following line-by-line:
```py
import subprocess
subprocess.run(["/challenge/embryoio_level18"], env={'mufyxw':'ulflmtrplg'}).stdout
```
`pwn.college{I7nFHTHbUgJQqk2Icu_cx_gfHHT.QX4EDLyYjNyEzW}`

## level19
```
$ echo "imsqsiru" > /tmp/xhekoe
```
Run `$ ipython` to enter `ipython` environment. Type the following line-by-line:
```py
import subprocess
with open("/tmp/xhekoe","r") as f:
    subprocess.run(["/challenge/embryoio_level19"], stdin=f).stdout
```
`pwn.college{8NdjJ9wG62VdsK-X2GRbuwURHn5.QX5EDLyYjNyEzW}`

## level20
Run `$ ipython` to enter `ipython` environment. Type the following line-by-line:
```py
import subprocess
with open("/tmp/hyzlrr","w") as f:
    subprocess.run(["/challenge/embryoio_level20"], stdout=f).stdout
```
Exit `ipython` with ctrl+d
```
$ cat /tmp/hyzlrr
```
`pwn.college{0-PA1l5BYhHwaeZA3Lx1kc7zN9g.QXwIDLyYjNyEzW}`

## level21
Run `$ ipython` to enter `ipython` environment. Type the following line-by-line:
```py
import subprocess
subprocess.run(["/challenge/embryoio_level21"], env={}).stdout
```
`pwn.college{wTL85PHLG_t94iT6gRIa6Sx69jR.QXxIDLyYjNyEzW}`

## level22
Create a file `solve.py` with the following:
```py
import subprocess
print(subprocess.run(["/challenge/embryoio_level22"]).stdout)
```
```
$ python solve.py
```
`pwn.college{EINDxzzcZ0Nrv6ayRUzv6bNQJni.QXyIDLyYjNyEzW}`

## level23
Create a file `solve.py` with the following:
```py
import subprocess
print(subprocess.run(["/challenge/embryoio_level23"]).stdout)
```
```
$ python solve.py
ofnkaxnz
```
`pwn.college{MkOEumixdG2inJi1n8MwKd8Qk37.QXzIDLyYjNyEzW}`

## level24
Create a file `solve.py` with the following:
```py
import subprocess
print(subprocess.run(["/challenge/embryoio_level24","hvcwkmqhxb"]).stdout)
```
```
$ python solve.py
```
`pwn.college{81_mWM183LrqLTocQla9HfCi392.QX0IDLyYjNyEzW}`

## level25
Create a file `solve.py` with the following:
```py
import subprocess
print(subprocess.run(["/challenge/embryoio_level25"],env={"dllbhc":"kmafnbxpyo"}).stdout)
```
```
$ python solve.py
```
`pwn.college{MK6ej5lg8_ZWkEGuaOLrFzizMbQ.QX1IDLyYjNyEzW}`

## level26
```
$ echo "mrbzkzud" > /tmp/vbekuz
```
Create a file `solve.py` with the following:
```py
import subprocess
with open("/tmp/vbekuz","r") as f:
    subprocess.run(["/challenge/embryoio_level26"], stdin=f).stdout
```
```
$ python solve.py
```
`pwn.college{U4InAeLSF8BAPcHquHHtwTnHUDC.QX2IDLyYjNyEzW}`

## level27
Create a file `solve.py` with the following:
```py
import subprocess
with open("/tmp/ewcmrr","w") as f:
    subprocess.run(["/challenge/embryoio_level27"], stdout=f).stdout
```
```
$ python solve.py
$ cat /tmp/ewcmrr
```
`pwn.college{EKiKOSlIUPEr4WicsNV7MDAlnwH.QX3IDLyYjNyEzW}`

## level28
Create a file `solve.py` with the following:
```py
import subprocess
subprocess.run(["/challenge/embryoio_level28"], env={}).stdout
```
```
$ python solve.py
```

## level29
Create a file `solve.c` with the following:
```c
#include <unistd.h>
#include <sys/types.h>
#include <sys/wait.h>
int pwncollege(){
    if (fork()==0) {
        execve("/challenge/embryoio_level29",0,0);
    }
    else {
        wait(0);
    }
    return 0;
}

int main() {
    return pwncollege();
}
```
```
$ gcc solve.c
$ ./a.out
```
`pwn.college{8iPB91OIDw_KyHn00KJTpA9yGWR.QX5IDLyYjNyEzW}`

## level30
Create a file `solve.c` with the following:
```c
#include <unistd.h>
#include <sys/types.h>
#include <sys/wait.h>
int pwncollege(){
    if (fork()==0) {
        execve("/challenge/embryoio_level30",0,0);
    }
    else {
        wait(0);
    }
    return 0;
}

int main() {
    return pwncollege();
}
```
```
$ gcc solve.c
$ ./a.out
qhwfjeao
```
`pwn.college{ksUeORNTcVvfx18SWGedtXvEJW-.QXwMDLyYjNyEzW}`

## level31
Create a file `solve.c` with the following:
```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/wait.h>
int pwncollege(){
    char * argv[] = {"a.out","niibxwjmor",NULL};
    if (fork()==0) {
        execve("/challenge/embryoio_level31",argv,0);
        perror("execve");
    }
    else {
        wait(0);
    }
    return 0;
}
int main() {
    return pwncollege();
}
```
```
$ gcc solve.c
$ ./a.out
```
`pwn.college{wWKKyT5szwvwkfcDWM8ICZYvY3S.QXxMDLyYjNyEzW}`

## level32
Create a file `solve.c` with the following:
```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/wait.h>
int pwncollege(){
    char * argv[] = {"a.out",NULL};
    char * envp[] = {"yrqads=lrwdwarwbs",NULL};
    if (fork()==0) {
        execve("/challenge/embryoio_level32",argv,envp);
        perror("execve");
    }
    else {
        wait(0);
    }
    return 0;
}

int main() {
    return pwncollege();
}
```
```
$ gcc solve.c
$ ./a.out
```
`pwn.college{QKImZmlI-v0Gftdp11RPoFgp9ZZ.QXyMDLyYjNyEzW}`